# BLOCKERS

- [S-01] CONTRACT-CHANGE: `core/types.py` is prepared on `agent-a/S-01-types-contract`, but no issue tracker is available to collect the required ACK comments from Agents B and C. Full `make check` is also blocked: this repository has no Makefile and Python 3.11 has no `mypy` module.
