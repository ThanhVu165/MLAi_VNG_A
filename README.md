# Escalation Referee

Trợ lý mô phỏng tiếp nhận, phân loại và xử lý email hành chính của sinh viên.

## Cài đặt

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install -r requirements.txt
make check
```
